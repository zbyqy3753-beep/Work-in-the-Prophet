/**
 * Base64-encoded audio clips — isolated so editors/AI can skip this file.
 */
export const SOUND_DATA = {
  // attack: "data:audio/wav;base64,...",
  // hit: "data:audio/wav;base64,...",
  // signal: "data:audio/wav;base64,...",
};
